{% extends 'base.html.twig' %}

{% block title %}Matériels{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  <style>
    .page-hero { display:flex; gap:12px; align-items:flex-end; }
    .hero-img { position:relative; flex:1; height:240px; border-radius:14px; overflow:hidden; box-shadow:0 6px 20px rgba(0,0,0,.08); }
    .hero-img img { width:100%; height:100%; object-fit:cover; filter:saturate(115%) contrast(105%); transition: transform .35s ease-out; will-change: transform; }
    .hero-img::after { content:''; position:absolute; inset:0; background:linear-gradient(90deg, rgba(0,0,0,.35), rgba(0,0,0,.05)); transition: transform .35s ease-out; }
    .hero-label { position:absolute; left:16px; bottom:14px; color:#fff; font-weight:700; font-size:1.6rem; display:flex; gap:8px; align-items:center; text-shadow:0 1px 2px rgba(0,0,0,.3); transition: transform .35s ease-out; background: linear-gradient(90deg, var(--brand-blue), var(--brand-red)); padding: 6px 12px; border-radius: 10px; }
    .page-hero:hover .hero-img img { transform: scale(1.08); }
    .page-hero:hover .hero-img::after { transform: translateY(-4px); }
    .page-hero:hover .hero-label { transform: translateY(-2px); }
    .hero-action { align-self:flex-end; height:44px; border-radius:10px; }
  </style>
{% endblock %}


{% block body %}
<div class="page-hero mb-3">
  <div class="hero-img">
    <img src="{{ asset('images/materiel.jpg') }}" alt="Matériels">
    <div class="hero-label"><i class="bi bi-tools"></i> Matériels</div>
  </div>
  <a href="{{ path('app_materiel_new') }}" class="btn btn-warning hero-action">Nouveau</a>
</div>

<div class="alert alert-dark mb-3">
  <div class="small">
    Recherchez par code, nom ou fournisseur. Si vous saisissez un nombre, la recherche inclut aussi la valeur HT. La recherche est insensible à la casse et partielle. Utilisez <span class="text-danger">Rechercher</span> pour appliquer le filtre et 
    <i class="bi bi-x-lg text-danger"></i> pour revenir à la liste complète. Les actions à droite permettent de modifier ou supprimer, une confirmation s’affiche avant la suppression. <span class="text-danger">La suppression peut être refusée si des dépendances existent.</span>
  </div>
</div>

 

<form method="get" class="card p-3 mb-3">
  <div class="row g-2">
    <div class="col-12">
       <div class="input-group">
          <span class="input-group-text"><i class="bi bi-search"></i></span>
          <input type="text" name="q" value="{{ q|default('') }}" class="form-control" placeholder="Rechercher un matériel (code, nom, fournisseur)..." autocomplete="off">
          <button class="btn btn-warning" type="submit">Rechercher</button>
          {% if q|default('') %}
              <a class="btn btn-outline-secondary" href="{{ path('app_materiel_index') }}"><i class="bi bi-x-lg"></i></a>
          {% endif %}
       </div>
    </div>
  </div>
</form>

<div class="card">
  <div class="table-responsive">
    <table class="table table-sm table-hover mb-0 table-striped table-bordered border-light">
      <thead>
      <tr>
        <th>Code</th>
        <th>Nom</th>
        <th>Fournisseur</th>
        <th>Valeur HT</th>
        <th class="text-end">Actions</th>
      </tr>
      </thead>
      <tbody>
      {% for m in materiels %}
        <tr>
          <td><span class="badge badge-code">{{ m.codeArticle }}</span></td>
          <td>
            {{ m.nom }} <br> 
            <span class="badge badge-code">{{ m.description }}</span> <br>
          </td>
          <td>{{ m.fournisseur ? m.fournisseur.nomFournisseur : '—' }}</td>
          <td>{{ m.valeurUnitaireHt }}</td>
          <td class="text-end">
            <a href="{{ path('app_materiel_edit', {'id': m.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
            <form method="post" action="{{ path('app_materiel_delete', {'id': m.id}) }}" class="d-inline">
              <input type="hidden" name="_token" value="{{ csrf_token('delete_materiel_' ~ m.id) }}">
              <button class="btn btn-sm btn-danger js-delete-btn" type="submit">Supprimer</button>
            </form>
          </td>
        </tr>
      {% else %}
        <tr>
          <td colspan="5" class="text-muted">Aucun matériel</td>
        </tr>
      {% endfor %}
      </tbody>
    </table>
  </div>
</div>

<div class="d-flex justify-content-end mt-3 gap-2">
  {% set nextPage = page + 1 %}
  {% set prevPage = page > 1 ? page - 1 : 1 %}
  <a class="btn btn-outline-secondary btn-sm" href="{{ path('app_materiel_index', app.request.query.all | merge({'page': prevPage})) }}">
    <i class="bi bi-chevron-left"></i> Précédent
  </a>
  <a class="btn btn-outline-secondary btn-sm" href="{{ path('app_materiel_index', app.request.query.all | merge({'page': nextPage})) }}">
    Suivant <i class="bi bi-chevron-right"></i>
  </a>
</div>
{% endblock %}
{#
  Fichier: materiel/index
  Objet: Liste, recherche et actions sur les matériels.
  Sections: en-tête, explication, recherche, tableau, pagination.
  Conseils: un nombre saisi filtre aussi la valeur HT.
#}
